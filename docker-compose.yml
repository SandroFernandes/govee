services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: govee-backend
    env_file:
      - .env
    volumes:
      - sqlite_data:/data
    ports:
      - "8000:8000"
    restart: unless-stopped

volumes:
  sqlite_data:
